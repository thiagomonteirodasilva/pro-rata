<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="prorata.thiagomonteiro.com.prorata">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pró-Rata"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.InicialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity"
            android:parentActivityName=".activity.InicialActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.InicialActivity"/>
        </activity>

        <activity android:name=".activity.PopSignificado"
            android:parentActivityName=".activity.InicialActivity"
            android:theme="@style/AppTheme.CustomTheme">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activity.InicialActivity"/>
        </activity>
    </application>

</manifest>